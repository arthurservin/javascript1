let loginForm = document.forms.login;

let emailInput = loginForm.email;

/////  everything after question mark (?) is a query of the DOM to the backend

//// %40 = @

//// ampersand (&) seperates perameters

/// in order to talk to a server there are 2 verbs = get or post
//  get = you are not sending any parameters, sending sending to url and getting something back (default method)
//  post = when you need to login with parameters













////
